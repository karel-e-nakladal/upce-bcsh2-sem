<Window x:Class="WpfApp1.View.AddParagraphView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Paragraph" Height="260" Width="200">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Paragraph" Margin="0,0,0,5"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="150">
                <TextBox x:Name="Paragraph" AcceptsReturn="True" TextWrapping="Wrap"/>
            </ScrollViewer>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Ok" Click="Ok" IsDefault="True" Margin="5" Padding="6"/>
                <Button Content="Cancel" IsCancel="True" Margin="5" Padding="6"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
