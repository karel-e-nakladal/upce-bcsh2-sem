<Window x:Class="WpfApp1.View.AddWorldView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.View"
        mc:Ignorable="d"
        Title="AddWorldView" Height="600" Width="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Create a New World"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="World name" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Name}" Height="28"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="Description" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Description}"
                     Height="90"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <Border Grid.Row="3"
                Padding="10"
                BorderThickness="1"
                CornerRadius="6"
                Margin="0,0,0,16">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEdited}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <StackPanel Orientation="Vertical" Margin="0,0,0,0">
                <StackPanel>
                    <Image Width="64"
                           Height="64"
                           Source="{Binding IconBitmap}"
                           Stretch="Uniform"
                           Margin="0,0,16,0"/>
                    <StackPanel>
                        <TextBlock Text="World icon"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Change icon"
                                    Width="140"
                                    Command="{Binding ChangeIconCommand}"/>
                            <Button Content="Delete icon"
                                    Width="100"
                                    Command="{Binding DeleteIconCommand}">
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel>
                    <Image Width="64"
                           Height="64"
                           Source="{Binding MapBitmap}"
                           Stretch="Uniform"
                           Margin="0,0,16,0"/>
                    <StackPanel>
                        <TextBlock Text="World map"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Change Map"
                                    Width="140"
                                    Command="{Binding ChangeMapCommand}"/>
                            <Button Content="Delete Map"
                                    Width="100"
                                    Command="{Binding DeleteMapCommand}">
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </StackPanel>

        </Border>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="100"
                    Margin="0,0,10,0"
                    IsCancel="True"/>
            <Button Width="100"
                    IsDefault="True"
                    Click="Ok">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Content" Value="Create"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEdited}" Value="True">
                                <Setter Property="Content" Value="Edit"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
