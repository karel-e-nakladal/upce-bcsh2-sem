<Window x:Class="WpfApp1.View.AddWorldView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.View"
        mc:Ignorable="d"
        Title="AddWorldView" Height="160" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name your world"/>
        <TextBox Grid.Row="1" Grid.Column="0" x:Name="Name"/>
        <TextBox TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="70" Grid.Row="2" Grid.Column="0" x:Name="Description"/>
        <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Ok"  Click="Ok" IsDefault="True" Margin="5" Width="100"/>
            <Button Content="Cancel" IsCancel="True" Margin="5" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
