<UserControl x:Class="WpfApp1.View.RichTextEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5" HorizontalAlignment="Left">
            <Button Content="Add Header" Command="{Binding HeaderCommand}" Margin="5" Padding="6"/>
            <Button Content="Add Paragraph" Command="{Binding ParagraphCommand}" Margin="5" Padding="6"/>
            <Button Content="Add Image" Command="{Binding ImageCommand}" Margin="5" Padding="6"/>
            <Button Content="Add Link" Command="{Binding LinkCommand}" Margin="5" Padding="6"/>
        </StackPanel>

        <!-- Editor -->
        <FlowDocumentScrollViewer Grid.Row="1" 
                                  VerticalScrollBarVisibility="Auto" 
                                  Margin="10" 
                                  x:Name="Editor" 
                                  Document="{Binding Document, Mode=TwoWay}"/>
    </Grid>
</UserControl>
