<UserControl x:Class="WpfApp1.View.RichTextEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5" >
            <Button Content="Add Header" Command="{Binding HeaderCommand}"/>
            <Button Content="Add Paragraph" Command="{Binding ParagraphCommand}"/>
            <Button Content="Add Image" Command="{Binding ImageCommand}"/>
            <Button Content="Add Link" Command="{Binding LinkCommand}"/>
        </StackPanel>
        <FlowDocumentScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <FlowDocument x:Name="Document">
            </FlowDocument>
        </FlowDocumentScrollViewer>
        
    </Grid>
</UserControl>
